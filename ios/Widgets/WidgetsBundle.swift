import SwiftUI
import WidgetKit

@main
struct YantraWidgets: WidgetBundle {
    var body: some Widget {
        FocusLiveActivity()
        FocusWidget()
        BhupuraWidget()
        TodayWidget()
        ListWidget()
        QuickAddWidget()
    }
}
